# GitHub MCP Server

# Shared settings
WORK_DIR=~/projects
READ_ONLY=false
LOG_FILE=mcp_audit.log
MCP_TRANSPORT=stdio
MCP_PORT=8080
MCP_SERVER_URL=https://your-tunnel.ngrok-free.dev
MCP_AUTH_PASSWORD=approve

# GitHub-specific: no additional env vars needed
# Uses your existing gh CLI authentication (gh auth login)
